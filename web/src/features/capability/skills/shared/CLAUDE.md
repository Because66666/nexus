# Skill 共享目录卡

- `skill-directory-card.tsx` 只统一能力页已安装、更新和社区 Skill 的卡片几何、数学曲线身份、点击层和内容层级。
- 标题、说明、徽标、元数据、动作可用性与命令仍由所属 `catalog/` 或 `external/` 域投影。
- 整卡详情动作使用独立覆盖按钮；右上角领域动作必须保留自己的按钮语义，不得嵌套交互元素。
- 卡片保持桌面三列、窄窗逐级收拢；说明最多两行，来源与使用信息只占一行。
