# Contacts 路由协调

- URL 查询参数是当前详情 Agent 的唯一选择状态，不维护镜像 state。
- `view=create` 是从全局联系人侧栏进入共享创建编辑器的短生命周期路由意图；小屏在编辑期间保持联系人内容层，关闭编辑器后清理该意图并返回联系人目录。
- DM 导航复用 direct-room target，页面不得重复 ensure + route 拼装协议。
- 路由协调器只消费具体命令结果，不持有编辑器或确认弹窗状态。
