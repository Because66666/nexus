# 列表原语

- 本目录拥有通用列表行与行内动作的结构和样式。
- `list-row-model.ts` 统一投影活动态、交互语义和根样式；`list-row.tsx` 只组合默认内容与调用方插槽。
- 活动行只使用中性活动底面和强文字，不绘制品牌色侧边标记；普通行保持透明，目录消费面可显式选择 `muted` 非活动文字层级，其他列表不得被连带降权。
- 列表资源、选择状态和命令生命周期由业务目录负责。
